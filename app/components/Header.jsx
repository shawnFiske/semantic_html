var React = require('react');

var Header = React.createClass({
  render: function() {
    
    return (
      <section>
        <header>
          <p>logo</p>
        </header>
        <nav>
          <p>nav</p>
        </nav>
      </section>
    )
  }
})

module.exports = Header;

